import { Meta, Story, Canvas, Preview, ArgsTable } from "@storybook/addon-docs/blocks"

import { Button } from "./Button"

<Meta title="molecules/Button" component={Button} />

# Button

## Styling for clickable elements

<Preview>
  <Story name="standard">
    <Button size="medium" label="Button" />
  </Story>
</Preview>

## Arguments

<ArgsTable of={Button} />

### Loading

Click to trigger a fake async call.

<Preview>
  <Story name="loading" classname="inset-flex">
    <Button label="Sync" onClickSync={() => {}} />
    <Button
      label="Async"
      onClickAsync={async () => {
        await new Promise((resolve) => setTimeout(resolve, 2000))
      }}
    />
  </Story>
</Preview>
