import React from "react"

// eslint-disable-next-line
export interface TemplateProps {}

/**
 *Copy this the folder to create a new component
 */
export const Template: React.FC<TemplateProps> = () => {
  return <div>Template</div>
}
